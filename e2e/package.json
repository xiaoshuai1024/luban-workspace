{
  "name": "@luban/e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Luban 跨项目流程性 E2E（engine→BFF→backend→website 全链路）",
  "scripts": {
    "install:e2e": "playwright install chrome",
    "install:e2e:ci": "playwright install chromium",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:engine": "playwright test --project=engine-flows",
    "@paused test:contract": "echo '@paused Go 后端已移除（commit e9b0abc），双后端契约测试暂停'",
    "@paused test:cross": "echo '@paused Go 后端已移除，test:cross 退化为 test:engine'",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.0.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.6.0"
  }
}
